name: Build Geosite
on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go environment
        uses: actions/setup-go@v5
        with:
          go-version: '1.24' # Обновили до версии, которую просит генератор
          cache: false

      - name: Install and Build
        run: |
          go install github.com/v2fly/domain-list-community@latest
          $(go env GOPATH)/bin/domain-list-community --datapath=./data --outputdir=./ --outputname=geosite.dat

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: geosite-file
          path: geosite.dat
